# Market Widget X Relay

Manual emergency fallback for public NFL reporter timelines used by Market Widget.

The managed feed now calls a quota-aware third-party proxy directly. This Nitter relay is intentionally not scheduled because public instances frequently block cloud data-center traffic. It stores no X credentials, cookies, or private extension code.
